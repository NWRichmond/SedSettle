function startSounds(~, ~)
%STARTSOUNDS Summary of this function goes here
%   Detailed explanation goes here
t = timer();
t.ExecutionMode = 'fixedRate';
t.TasksToExecute = 5;
t.Period = 5;
t.StartFcn = @(~,thisEvent)beep
beep
beep 
beep
load gong.mat;
sound(y);
end

